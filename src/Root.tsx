import MainPage from "./pages/MainPage/MainPage";
import { Container } from "react-bootstrap";

const Root = () => {
  return (
    <Container>
      <div>
        <MainPage />
      </div>
    </Container>
  );
};

export default Root;
